anthropic>=1.2.0
# The neural voice ships with the app rather than relying on the host OS.
# Voice models are installed by `python setup_voices.py` into voices/.
piper-tts>=1.7.0
fastapi>=0.110
uvicorn[standard]>=0.27

# Development only
pytest>=8.0
# For fastapi.testclient only. The Anthropic SDK uses httpx2, which it pulls in
# itself - do not add httpx expecting the SDK to use it.
httpx>=0.27

# Optional: PDF attachments. Everything else (docx, text, links, photos)
# needs no extra package; without this a PDF fails with a message saying so.
pypdf>=4.0
